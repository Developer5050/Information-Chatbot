const express = require("express")
const router = express.Router();

// const chatMessages = require('../controller/chatController');

// router.route('/messages').post(chatMessages);

module.exports = router;